#ifndef __HELP_HPP__
#define __HELP_HPP__

#include <opencv2/opencv.hpp>

const cv::String keys =
    "{ help h       |   | print this message                }"
    "{ file f       | 0 | Type of path: file                }"
    "{ device d     | 0 | Type of path: device              }"
    "{ path p       |   | path to device or file -p=<path>  }";

#endif /* __HELP_HPP__ */
